:root {
    --black: #050505;
    --red: #b30000;
    --blue: #0f4c81;
    --white: #f5f5f5;
    --dark-gray: #1a1a1a;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    background: var(--black);
    color: var(--white);
    font-family: 'Poppins', sans-serif;
    overflow-x: hidden;
    line-height: 1.6;
}

/* SCROLLBAR */
::-webkit-scrollbar {
    width: 10px;
}

::-webkit-scrollbar-track {
    background: var(--black);
}

::-webkit-scrollbar-thumb {
    background: var(--blue);
    border-radius: 20px;
}

::-webkit-scrollbar-thumb:hover {
    background: var(--red);
}

/* NAVBAR */
.navbar {
    position: fixed;
    top: 0;
    width: 100%;
    height: auto;
    padding: 15px 50px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 999;
    background: linear-gradient(to bottom, rgba(5, 5, 5, 0.98), rgba(5, 5, 5, 0.8));
    backdrop-filter: blur(15px);
    border-bottom: 1px solid rgba(15, 76, 129, 0.1);
    transition: 0.3s ease;
}

.navbar-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
}

.navbar-logo {
    display: flex;
    align-items: center;
    gap: 15px;
    cursor: pointer;
}

.logo-img {
    width: 50px;
    height: 50px;
    object-fit: contain;
    transition: 0.3s ease;
}

.logo-img:hover {
    filter: drop-shadow(0 0 15px var(--blue));
}

.logo-text {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 18px;
    letter-spacing: 2px;
    color: var(--white);
    font-weight: bold;
}

.nav-menu {
    display: flex;
    list-style: none;
    gap: 40px;
}

.nav-link {
    color: var(--white);
    text-decoration: none;
    font-size: 14px;
    font-weight: 500;
    position: relative;
    letter-spacing: 1px;
    transition: 0.3s ease;
    text-transform: uppercase;
}

.nav-link::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(to right, var(--blue), var(--red));
    transition: 0.3s ease;
}

.nav-link:hover {
    color: var(--blue);
}

.nav-link:hover::after {
    width: 100%;
}

.hamburger {
    display: none;
    flex-direction: column;
    cursor: pointer;
}

.hamburger span {
    width: 25px;
    height: 3px;
    background: var(--white);
    margin: 5px 0;
    transition: 0.3s ease;
    border-radius: 2px;
}

/* CONTAINER */
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/* HERO SECTION */
.hero {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 80px;
}

.hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
}

.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
        to bottom,
        rgba(5, 5, 5, 0.4),
        rgba(5, 5, 5, 0.8),
        rgba(5, 5, 5, 0.95)
    );
    z-index: 2;
}

.hero-content {
    position: relative;
    z-index: 3;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 30px;
}

.hero-logo {
    width: 150px;
    height: 150px;
    object-fit: contain;
    animation: float 4s ease-in-out infinite;
    filter: drop-shadow(0 0 30px rgba(15, 76, 129, 0.5));
}

.hero-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 120px;
    letter-spacing: 12px;
    line-height: 1;
    text-shadow: 0 0 40px rgba(0, 0, 0, 0.9), 0 0 80px rgba(15, 76, 129, 0.3);
    color: var(--white);
    text-transform: uppercase;
}

.hero-subtitle {
    font-size: 28px;
    color: #e0e0e0;
    letter-spacing: 2px;
    font-weight: 300;
    text-shadow: 0 0 20px rgba(0, 0, 0, 0.8);
    max-width: 700px;
}

.hero-btn {
    background: linear-gradient(135deg, var(--red), #ff3333);
    color: var(--white);
    border: none;
    padding: 18px 50px;
    font-size: 16px;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    cursor: pointer;
    border-radius: 5px;
    transition: 0.4s ease;
    display: flex;
    align-items: center;
    gap: 15px;
    box-shadow: 0 0 30px rgba(179, 0, 0, 0.4);
    text-decoration: none;
}

.hero-btn:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 0 0 50px rgba(179, 0, 0, 0.7), 0 0 80px rgba(15, 76, 129, 0.3);
    background: linear-gradient(135deg, #ff3333, #ff6666);
}

.btn-text {
    display: inline;
}

.btn-icon {
    font-size: 14px;
    transition: 0.3s ease;
}

.hero-btn:hover .btn-icon {
    transform: scale(1.3);
}

.hero-scroll {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 4;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
}

.scroll-text {
    font-size: 12px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.6);
}

.scroll-arrow {
    display: flex;
    flex-direction: column;
    gap: 5px;
}

.scroll-arrow span {
    width: 2px;
    height: 10px;
    background: var(--blue);
    animation: bounce 2s infinite;
}

.scroll-arrow span:nth-child(2) {
    animation-delay: 0.2s;
}

.scroll-arrow span:nth-child(3) {
    animation-delay: 0.4s;
}

/* MODAL VIDEO PLAYER */
.modal {
    display: none;
    position: fixed;
    z-index: 2000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.98);
    animation: fadeIn 0.3s ease;
}

.modal.active {
    display: flex;
    align-items: center;
    justify-content: center;
}

.modal-content {
    position: relative;
    background-color: var(--black);
    padding: 30px;
    max-width: 90%;
    max-height: 90vh;
    border-radius: 10px;
    overflow: auto;
}

#mainVideo {
    width: 100%;
    max-width: 1000px;
    max-height: 70vh;
    border-radius: 5px;
    box-shadow: 0 0 40px rgba(15, 76, 129, 0.3);
}

.video-info {
    margin-top: 20px;
    text-align: center;
}

.video-info h2 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 32px;
    letter-spacing: 2px;
    color: var(--blue);
    margin-bottom: 10px;
}

.video-info p {
    color: #999;
    font-size: 14px;
}

.close {
    position: absolute;
    right: 30px;
    top: 15px;
    color: var(--white);
    font-size: 40px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s ease;
    z-index: 2001;
}

.close:hover {
    color: var(--red);
    transform: scale(1.2);
}

/* SECTIONS */
section {
    padding: 100px 50px;
    position: relative;
    overflow: hidden;
}

.section-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 48px;
    letter-spacing: 8px;
    color: var(--blue);
    text-transform: uppercase;
    margin-bottom: 60px;
    text-align: center;
}

.section-title::after {
    content: '';
    display: block;
    width: 80px;
    height: 4px;
    background: linear-gradient(to right, var(--blue), var(--red));
    margin: 20px auto 0;
    border-radius: 2px;
}

/* ABOUT SECTION */
.about {
    background: linear-gradient(135deg, rgba(5, 5, 5, 0.95) 0%, rgba(15, 76, 129, 0.05) 100%);
}

.about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    margin-bottom: 60px;
}

.about-text {
    background: rgba(20, 20, 20, 0.8);
    padding: 40px;
    border-radius: 10px;
    border-left: 4px solid var(--blue);
    backdrop-filter: blur(10px);
}

.about-text h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 32px;
    letter-spacing: 3px;
    color: var(--red);
    margin-bottom: 20px;
    text-transform: uppercase;
}

.about-text p {
    color: #d1d1d1;
    line-height: 2;
    font-size: 16px;
}

.about-message {
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.1) 0%, rgba(179, 0, 0, 0.1) 100%);
    border: 2px solid rgba(15, 76, 129, 0.3);
    padding: 50px;
    border-radius: 10px;
    text-align: center;
}

.about-message blockquote p {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 36px;
    letter-spacing: 2px;
    color: var(--white);
    font-style: italic;
    line-height: 1.8;
}

/* CAST SECTION */
.cast {
    background: linear-gradient(135deg, rgba(5, 5, 5, 0.95) 0%, rgba(179, 0, 0, 0.05) 100%);
}

.cast-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 40px;
}

.cast-card {
    position: relative;
    overflow: hidden;
    border-radius: 10px;
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.95), rgba(5, 5, 5, 0.95));
    border: 1px solid rgba(15, 76, 129, 0.2);
    transition: 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    backdrop-filter: blur(10px);
}

.cast-image {
    width: 100%;
    height: 350px;
    overflow: hidden;
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.2), rgba(179, 0, 0, 0.2));
}

.cast-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: 0.5s ease;
}

.cast-card:hover .cast-image img {
    transform: scale(1.08);
    filter: brightness(1.2);
}

.cast-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.15), rgba(179, 0, 0, 0.15));
    opacity: 0;
    transition: 0.5s ease;
    z-index: 5;
}

.cast-card:hover::before {
    opacity: 1;
}

.cast-card:hover {
    transform: translateY(-15px) scale(1.02);
    box-shadow: 
        0 10px 40px rgba(15, 76, 129, 0.3),
        0 0 60px rgba(179, 0, 0, 0.2);
    border-color: rgba(15, 76, 129, 0.5);
}

.cast-name {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 24px;
    letter-spacing: 2px;
    color: var(--white);
    margin: 20px 20px 0;
    text-transform: uppercase;
}

.cast-role {
    color: var(--blue);
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin: 10px 20px 20px;
    font-weight: 600;
}

/* PREVENTION SECTION */
.prevention {
    background: linear-gradient(135deg, rgba(5, 5, 5, 0.98) 0%, rgba(15, 76, 129, 0.05) 100%);
}

.prevention-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
    margin-bottom: 60px;
}

.prevention-card {
    position: relative;
    padding: 35px;
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.95), rgba(5, 5, 5, 0.95));
    border: 1px solid rgba(15, 76, 129, 0.2);
    border-radius: 10px;
    transition: 0.4s ease;
    backdrop-filter: blur(10px);
    overflow: hidden;
}

.prevention-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.1), rgba(179, 0, 0, 0.1));
    opacity: 0;
    transition: 0.4s ease;
}

.prevention-card:hover::before {
    opacity: 1;
}

.prevention-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 40px rgba(15, 76, 129, 0.25);
}

.card-icon {
    font-size: 48px;
    margin-bottom: 20px;
}

.prevention-card h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 28px;
    letter-spacing: 2px;
    color: var(--red);
    margin-bottom: 20px;
    text-transform: uppercase;
}

.prevention-card ul {
    list-style: none;
}

.prevention-card li {
    color: #cfcfcf;
    margin-bottom: 12px;
    padding-left: 20px;
    position: relative;
    font-size: 14px;
    line-height: 1.8;
}

.prevention-card li::before {
    content: 'â–¸';
    position: absolute;
    left: 0;
    color: var(--blue);
}

.prevention-message {
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.1), rgba(179, 0, 0, 0.1));
    border: 2px solid rgba(15, 76, 129, 0.3);
    padding: 40px;
    border-radius: 10px;
    text-align: center;
}

.prevention-message h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 32px;
    letter-spacing: 2px;
    color: var(--blue);
    margin-bottom: 15px;
}

.prevention-message p {
    font-size: 18px;
    color: #d1d1d1;
    line-height: 1.8;
}

/* FINAL MESSAGE SECTION */
.final-message {
    position: relative;
    height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(
        180deg,
        rgba(5, 5, 5, 0.95) 0%,
        rgba(15, 76, 129, 0.1) 50%,
        rgba(5, 5, 5, 0.95) 100%
    );
    overflow: hidden;
}

.final-message::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(15, 76, 129, 0.1), transparent 70%);
    z-index: 1;
}

.final-content {
    position: relative;
    z-index: 2;
    text-align: center;
}

.final-content h2 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 72px;
    letter-spacing: 8px;
    color: var(--white);
    margin-bottom: 30px;
    text-transform: uppercase;
    text-shadow: 0 0 40px rgba(15, 76, 129, 0.3);
}

.final-content p {
    font-size: 32px;
    letter-spacing: 4px;
    color: var(--red);
    text-transform: uppercase;
    font-weight: bold;
    text-shadow: 0 0 20px rgba(0, 0, 0, 0.9);
}

.final-line {
    width: 100px;
    height: 3px;
    background: linear-gradient(to right, transparent, var(--blue), transparent);
    margin: 40px auto 0;
    border-radius: 2px;
}

/* FOOTER */
.footer {
    background: linear-gradient(to bottom, rgba(5, 5, 5, 0.95), rgba(0, 0, 0, 0.99));
    border-top: 1px solid rgba(15, 76, 129, 0.2);
    padding: 60px 50px 30px;
}

.footer-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 40px;
    margin-bottom: 40px;
}

.footer-section h4 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 20px;
    letter-spacing: 2px;
    color: var(--blue);
    margin-bottom: 15px;
    text-transform: uppercase;
}

.footer-section p {
    color: #999;
    font-size: 14px;
    line-height: 1.8;
}

.footer-section ul {
    list-style: none;
}

.footer-section li {
    color: #999;
    font-size: 14px;
    margin-bottom: 10px;
    transition: 0.3s ease;
}

.footer-section li:hover {
    color: var(--red);
    padding-left: 5px;
}

.social-links {
    display: flex;
    gap: 20px;
    margin-top: 15px;
}

.social-links a {
    font-size: 24px;
    transition: 0.3s ease;
    text-decoration: none;
}

.social-links a:hover {
    transform: translateY(-5px) scale(1.2);
    filter: drop-shadow(0 0 10px var(--blue));
}

.footer-bottom {
    text-align: center;
    padding-top: 30px;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    color: #666;
    font-size: 12px;
}

/* ANIMATIONS */
@keyframes float {
    0%, 100% {
        transform: translateY(0);
    }
    50% {
        transform: translateY(-20px);
    }
}

@keyframes bounce {
    0%, 100% {
        transform: translateY(0);
        opacity: 0.5;
    }
    50% {
        transform: translateY(5px);
        opacity: 1;
    }
}

@keyframes fadeIn {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* AOS ANIMATIONS */
[data-aos] {
    opacity: 0;
    transition-property: opacity, transform;
    transition-duration: 0.6s;
    transition-timing-function: ease-in-out;
}

[data-aos].aos-animate {
    opacity: 1;
}

[data-aos~="fade-up"] {
    transform: translateY(30px);
}

[data-aos~="fade-up"].aos-animate {
    transform: translateY(0);
}

[data-aos~="fade-down"] {
    transform: translateY(-30px);
}

[data-aos~="fade-down"].aos-animate {
    transform: translateY(0);
}

[data-aos~="fade-left"] {
    transform: translateX(-30px);
}

[data-aos~="fade-left"].aos-animate {
    transform: translateX(0);
}

[data-aos~="fade-right"] {
    transform: translateX(30px);
}

[data-aos~="fade-right"].aos-animate {
    transform: translateX(0);
}

[data-aos~="zoom-in"] {
    transform: scale(0.9);
}

[data-aos~="zoom-in"].aos-animate {
    transform: scale(1);
}

/* RESPONSIVE */
@media (max-width: 768px) {
    .navbar {
        padding: 15px 20px;
    }
    
    .nav-menu {
        display: none;
    }
    
    .hamburger {
        display: flex;
    }
    
    .hero {
        margin-top: 60px;
    }
    
    .hero-title {
        font-size: 60px;
        letter-spacing: 4px;
    }
    
    .hero-subtitle {
        font-size: 18px;
        letter-spacing: 1px;
    }
    
    .hero-logo {
        width: 100px;
        height: 100px;
    }
    
    section {
        padding: 60px 20px;
    }
    
    .section-title {
        font-size: 32px;
        letter-spacing: 4px;
    }
    
    .about-content {
        grid-template-columns: 1fr;
        gap: 30px;
    }
    
    .about-text {
        padding: 25px;
    }
    
    .about-text h3 {
        font-size: 24px;
    }
    
    .cast-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .prevention-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .final-content h2 {
        font-size: 42px;
        letter-spacing: 4px;
    }
    
    .final-content p {
        font-size: 20px;
    }
    
    .footer-container {
        gap: 30px;
    }
    
    .hero-btn {
        width: 100%;
        justify-content: center;
    }
}

@media (max-width: 480px) {
    .hero-title {
        font-size: 36px;
    }
    
    .hero-subtitle {
        font-size: 14px;
    }
    
    .section-title {
        font-size: 24px;
    }
    
    .about-text h3 {
        font-size: 18px;
    }
    
    .prevention-card h3 {
        font-size: 20px;
    }
    
    .final-content h2 {
        font-size: 28px;
    }
    
    .final-content p {
        font-size: 16px;
    }
}

/* HERO */

.hero{
    position:relative;
    width:100%;
    height:100vh;
    overflow:hidden;
}

.hero-video{
    position:absolute;
    width:100%;
    height:100%;
    object-fit:cover;
}

.overlay{
    position:absolute;
    inset:0;
    background:
    linear-gradient(
        to bottom,
        rgba(0,0,0,0.55),
        rgba(0,0,0,0.85)
    );
}

.hero-content{
    position:relative;
    z-index:2;
    height:100%;
    display:flex;
    flex-direction:column;
    justify-content:center;
    align-items:center;
    text-align:center;
    padding:20px;
}

.main-logo{
    width:240px;
    margin-bottom:20px;
    animation:float 4s ease-in-out infinite;
}

.hero-mini-text{
    color:#b30000;
    letter-spacing:4px;
    font-size:13px;
    margin-bottom:20px;
    font-weight:600;
}

.hero-content h1{
    font-family:'Bebas Neue',sans-serif;
    font-size:120px;
    letter-spacing:8px;
    line-height:1;
    text-shadow:0 0 30px rgba(0,0,0,0.8);
}

.hero-content p{
    margin-top:20px;
    font-size:24px;
    color:#d7d7d7;
    max-width:700px;
}

.hero-buttons{
    margin-top:40px;
    display:flex;
    gap:20px;
    flex-wrap:wrap;
    justify-content:center;
}

.btn-primary,
.btn-secondary{
    padding:15px 35px;
    border-radius:10px;
    text-decoration:none;
    font-weight:600;
    transition:0.3s;
}

.btn-primary{
    background:#b30000;
    color:white;
}

.btn-primary:hover{
    background:#ff0000;
    transform:translateY(-5px);
    box-shadow:0 0 25px rgba(179,0,0,0.5);
}

.btn-secondary{
    border:1px solid rgba(255,255,255,0.2);
    color:white;
    background:rgba(255,255,255,0.05);
    backdrop-filter:blur(8px);
}

.btn-secondary:hover{
    background:rgba(255,255,255,0.12);
    transform:translateY(-5px);
}

/* SECCIONES */

section{
    padding:130px 10%;
}

.section-title{
    margin-bottom:60px;
}

.section-subtitle{
    color:#b30000;
    letter-spacing:4px;
    font-size:13px;
    font-weight:600;
}

.section-title h2{
    margin-top:12px;
    font-size:60px;
    font-family:'Bebas Neue',sans-serif;
    letter-spacing:3px;
    color:#0f4c81;
}

/* SOBRE */

.sobre-content{
    max-width:950px;
}

.sobre-content p{
    color:#d1d1d1;
    line-height:2;
    margin-bottom:25px;
    font-size:17px;
}

/* VIDEO */

.video-wrapper{
    display:flex;
    justify-content:center;
}

.video-wrapper video{
    width:100%;
    max-width:1200px;
    border-radius:18px;
    overflow:hidden;
    box-shadow:
    0 0 40px rgba(0,0,0,0.7),
    0 0 60px rgba(15,76,129,0.15);
}

/* REPARTO */

.cast-container{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
    gap:30px;
}

.cast-card{
    position:relative;
    padding:45px 35px;
    border-radius:18px;

    background:
    linear-gradient(
        145deg,
        rgba(15,15,15,0.95),
        rgba(5,5,5,0.95)
    );

    border:1px solid rgba(255,255,255,0.05);

    overflow:hidden;

    transition:0.4s ease;

    backdrop-filter:blur(10px);
}

.cast-card::before{
    content:"";
    position:absolute;
    inset:0;

    background:
    linear-gradient(
        135deg,
        rgba(15,76,129,0.12),
        rgba(179,0,0,0.10)
    );

    opacity:0;

    transition:0.4s;
}

.cast-card:hover::before{
    opacity:1;
}

.cast-card:hover{
    transform:translateY(-10px);

    box-shadow:
    0 0 25px rgba(15,76,129,0.25),
    0 0 40px rgba(179,0,0,0.15);
}

.cast-line{
    width:60px;
    height:4px;

    background:
    linear-gradient(to right,#0f4c81,#b30000);

    border-radius:20px;

    margin-bottom:25px;
}

.cast-role{
    display:block;

    color:#9d9d9d;

    font-size:14px;

    letter-spacing:2px;

    text-transform:uppercase;

    margin-bottom:12px;
}

.cast-card h3{
    font-size:34px;

    font-family:'Bebas Neue',sans-serif;

    letter-spacing:2px;

    color:white;
}

/* PREVENCIÃ“N */

.prevention-grid{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
    gap:30px;
}

.prevention-card{
    padding:40px;
    border-radius:18px;

    background:
    linear-gradient(
        145deg,
        rgba(18,18,18,0.95),
        rgba(8,8,8,0.95)
    );

    border:1px solid rgba(255,255,255,0.05);

    transition:0.4s;
}

.prevention-card:hover{
    transform:translateY(-10px);

    box-shadow:
    0 0 30px rgba(15,76,129,0.18);
}

.prevention-card h3{
    margin-bottom:18px;

    font-size:32px;

    font-family:'Bebas Neue',sans-serif;

    color:#0f4c81;

    letter-spacing:2px;
}

.prevention-card p{
    color:#cfcfcf;
    line-height:1.9;
}

/* FINAL */

.final-section{
    position:relative;
    height:55vh;

    display:flex;
    justify-content:center;
    align-items:center;

    text-align:center;

    overflow:hidden;
}

.final-overlay{
    position:absolute;
    inset:0;

    background:
    radial-gradient(
        circle,
        rgba(15,76,129,0.10),
        rgba(0,0,0,0.95)
    );
}

.final-content{
    position:relative;
    z-index:2;
}

.final-content h2{
    font-size:75px;
    font-family:'Bebas Neue',sans-serif;
    letter-spacing:4px;
    margin-bottom:20px;
}

.final-content p{
    color:#b30000;
    letter-spacing:3px;
    font-size:18px;
}

/* FOOTER */

footer{
    padding:35px;
    text-align:center;

    border-top:1px solid rgba(255,255,255,0.05);

    color:#777;

    font-size:14px;
}

/* REVEAL */

.reveal{
    opacity:0;
    transform:translateY(80px);
    transition:all 1.2s ease;
}

.reveal.active{
    opacity:1;
    transform:translateY(0);
}

/* ANIMACIONES */

@keyframes float{

    0%{
        transform:translateY(0px);
    }

    50%{
        transform:translateY(-10px);
    }

    100%{
        transform:translateY(0px);
    }

}

/* RESPONSIVE */

@media(max-width:1000px){

    .hero-content h1{
        font-size:80px;
    }

    .section-title h2{
        font-size:45px;
    }

    .final-content h2{
        font-size:50px;
    }

}

@media(max-width:700px){

    .navbar{
        padding:20px;
        flex-direction:column;
        gap:15px;
    }

    .navbar nav{
        gap:18px;
        flex-wrap:wrap;
        justify-content:center;
    }

    .hero-content h1{
        font-size:55px;
    }

    .hero-content p{
        font-size:18px;
    }

    .main-logo{
        width:170px;
    }

    .section-title h2{
        font-size:38px;
    }

    .final-content h2{
        font-size:36px;
    }

    .btn-primary,
    .btn-secondary{
        width:100%;
        text-align:center;
    }

}

/* PAGE HEADER */
.page-header {
    position: relative;
    height: 50vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.2) 0%, rgba(179, 0, 0, 0.2) 100%);
    overflow: hidden;
    margin-top: 80px;
}

.header-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(5, 5, 5, 0.3), rgba(5, 5, 5, 0.9));
}

.header-content {
    position: relative;
    z-index: 2;
    text-align: center;
}

.header-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 64px;
    letter-spacing: 8px;
    color: var(--white);
    margin-bottom: 20px;
    text-transform: uppercase;
    text-shadow: 0 0 40px rgba(15, 76, 129, 0.3);
}

.header-subtitle {
    font-size: 24px;
    color: #d1d1d1;
    letter-spacing: 2px;
}

/* CONTENT SECTION */
.content-section {
    padding: 80px 50px;
}

.content-block {
    margin-bottom: 80px;
}

.content-block h2 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 40px;
    letter-spacing: 4px;
    color: var(--blue);
    margin-bottom: 20px;
    text-transform: uppercase;
}

.block-separator {
    width: 60px;
    height: 3px;
    background: linear-gradient(to right, var(--blue), var(--red));
    margin-bottom: 30px;
    border-radius: 2px;
}

.content-block p {
    color: #d1d1d1;
    font-size: 16px;
    line-height: 2;
    margin-bottom: 25px;
}

/* CAST LIST */
.cast-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 25px;
    margin-top: 40px;
}

.cast-member {
    background: linear-gradient(145deg, rgba(15, 76, 129, 0.15), rgba(179, 0, 0, 0.1));
    border: 1px solid rgba(15, 76, 129, 0.3);
    padding: 30px;
    border-radius: 10px;
    transition: 0.4s ease;
    text-align: center;
}

.cast-member:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 40px rgba(15, 76, 129, 0.25);
    border-color: rgba(15, 76, 129, 0.6);
}

.cast-member h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 20px;
    letter-spacing: 2px;
    color: var(--white);
    margin-bottom: 8px;
    text-transform: uppercase;
}

.cast-role {
    color: var(--red);
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
    font-weight: 600;
    margin-bottom: 15px !important;
}

.cast-description {
    color: #cfcfcf;
    font-size: 13px;
    line-height: 1.8;
    margin: 0 !important;
}

/* WARNING GRID */
.warning-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 25px;
    margin-top: 40px;
}

.warning-card {
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.95), rgba(5, 5, 5, 0.95));
    border: 1px solid rgba(15, 76, 129, 0.2);
    padding: 30px;
    border-radius: 10px;
    text-align: center;
    transition: 0.4s ease;
}

.warning-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 40px rgba(15, 76, 129, 0.25);
    border-color: rgba(15, 76, 129, 0.5);
}

.warning-icon {
    font-size: 48px;
    display: block;
    margin-bottom: 15px;
}

.warning-card h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 18px;
    letter-spacing: 2px;
    color: var(--red);
    margin-bottom: 12px;
    text-transform: uppercase;
}

.warning-card p {
    font-size: 14px;
    color: #cfcfcf;
}

/* CONSEQUENCES LIST */
.consequences-list {
    margin-top: 40px;
}

.consequence-item {
    display: flex;
    gap: 25px;
    margin-bottom: 30px;
    padding: 25px;
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.8), rgba(5, 5, 5, 0.8));
    border-left: 4px solid var(--red);
    border-radius: 5px;
    transition: 0.3s ease;
}

.consequence-item:hover {
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.95), rgba(5, 5, 5, 0.95));
    transform: translateX(10px);
}

.consequence-number {
    flex-shrink: 0;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--blue), var(--red));
    border-radius: 50%;
    font-weight: bold;
    font-size: 20px;
}

.consequence-item h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 18px;
    letter-spacing: 2px;
    color: var(--white);
    margin-bottom: 8px;
}

.consequence-item p {
    font-size: 14px;
    color: #cfcfcf;
    margin: 0;
}

/* ACTION GRID */
.action-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 20px;
    margin-top: 40px;
}

.action-step {
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.95), rgba(5, 5, 5, 0.95));
    border: 1px solid rgba(15, 76, 129, 0.2);
    padding: 30px;
    border-radius: 10px;
    text-align: center;
    transition: 0.4s ease;
}

.action-step:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 40px rgba(179, 0, 0, 0.2);
}

.step-number {
    display: inline-flex;
    width: 50px;
    height: 50px;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--red), #ff3333);
    border-radius: 50%;
    font-weight: bold;
    font-size: 24px;
    margin-bottom: 15px;
}

.action-step h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 18px;
    letter-spacing: 2px;
    color: var(--white);
    margin-bottom: 12px;
    text-transform: uppercase;
}

.action-step p {
    font-size: 13px;
    color: #cfcfcf;
    line-height: 1.8;
}

/* RESOURCES GRID */
.resources-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
    margin-top: 40px;
}

.resource-card {
    background: linear-gradient(145deg, rgba(15, 76, 129, 0.1), rgba(179, 0, 0, 0.1));
    border: 2px solid rgba(15, 76, 129, 0.3);
    padding: 30px;
    border-radius: 10px;
    transition: 0.4s ease;
}

.resource-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 50px rgba(15, 76, 129, 0.25);
    border-color: rgba(15, 76, 129, 0.6);
}

.resource-card h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 20px;
    letter-spacing: 2px;
    color: var(--blue);
    margin-bottom: 12px;
    text-transform: uppercase;
}

.resource-number {
    font-size: 16px;
    color: var(--red);
    font-weight: bold;
    margin-bottom: 15px;
    display: block;
}

.resource-card p {
    font-size: 14px;
    color: #d1d1d1;
    margin-bottom: 12px;
}

.resource-info {
    font-size: 12px;
    color: #999;
    font-style: italic;
    margin-top: 15px;
    padding-top: 15px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

/* WITNESS ACTIONS */
.witness-actions {
    margin-top: 40px;
}

.witness-item {
    display: flex;
    gap: 25px;
    margin-bottom: 25px;
    padding: 25px;
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.8), rgba(5, 5, 5, 0.8));
    border-left: 4px solid var(--blue);
    border-radius: 5px;
    transition: 0.3s ease;
}

.witness-item:hover {
    transform: translateX(10px);
    background: linear-gradient(145deg, rgba(15, 15, 15, 0.95), rgba(5, 5, 5, 0.95));
}

.witness-icon {
    font-size: 36px;
    flex-shrink: 0;
}

.witness-item h3 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 16px;
    letter-spacing: 2px;
    color: var(--white);
    margin-bottom: 8px;
}

.witness-item p {
    font-size: 14px;
    color: #cfcfcf;
    margin: 0;
}

/* FINAL MESSAGE BLOCK */
.final-message-block {
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.15), rgba(179, 0, 0, 0.15));
    border: 2px solid rgba(15, 76, 129, 0.4);
    padding: 50px;
    border-radius: 10px;
    text-align: center;
    margin-top: 60px;
}

.final-message-block h2 {
    color: var(--red);
    font-size: 36px;
    margin-bottom: 30px;
}

.final-quotes {
    display: flex;
    flex-direction: column;
    gap: 20px;
}

.quote-main {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 28px;
    letter-spacing: 3px;
    color: var(--white);
    line-height: 1.8;
}

.quote-secondary {
    font-size: 16px;
    color: #d1d1d1;
    line-height: 1.8;
}

/* CTA SECTION */
.cta-section {
    background: linear-gradient(135deg, rgba(15, 76, 129, 0.2), rgba(179, 0, 0, 0.2));
    padding: 60px 50px;
    text-align: center;
}

.cta-section h2 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 48px;
    letter-spacing: 6px;
    color: var(--white);
    margin-bottom: 15px;
}

.cta-section p {
    font-size: 18px;
    color: #d1d1d1;
    margin-bottom: 30px;
}

.cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
}

.btn-primary,
.btn-secondary {
    padding: 15px 40px;
    border-radius: 5px;
    text-decoration: none;
    font-weight: 700;
    letter-spacing: 2px;
    transition: 0.3s ease;
    text-transform: uppercase;
    font-size: 14px;
}

.btn-primary {
    background: linear-gradient(135deg, var(--red), #ff3333);
    color: var(--white);
    border: none;
}

.btn-primary:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(179, 0, 0, 0.4);
}

.btn-secondary {
    background: transparent;
    color: var(--blue);
    border: 2px solid var(--blue);
}

.btn-secondary:hover {
    background: rgba(15, 76, 129, 0.2);
    transform: translateY(-5px);
}

/* RESPONSIVE FOR CONTENT PAGES */
@media (max-width: 768px) {
    .page-header {
        height: 35vh;
        margin-top: 60px;
    }
    
    .header-title {
        font-size: 36px;
        letter-spacing: 4px;
    }
    
    .header-subtitle {
        font-size: 16px;
    }
    
    .content-section {
        padding: 60px 20px;
    }
    
    .content-block h2 {
        font-size: 28px;
    }
    
    .warning-grid {
        grid-template-columns: 1fr;
        gap: 15px;
    }
    
    .consequences-list {
        margin-top: 20px;
    }
    
    .consequence-item {
        flex-direction: column;
        text-align: center;
        padding: 20px;
    }
    
    .consequence-number {
        margin: 0 auto 15px;
    }
    
    .cast-list {
        grid-template-columns: 1fr;
        gap: 15px;
    }
    
    .cast-member {
        padding: 20px;
    }
    
    .cast-member h3 {
        font-size: 16px;
    }
    
    .action-grid {
        grid-template-columns: 1fr;
    }
    
    .resources-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .witness-item {
        flex-direction: column;
        text-align: center;
    }
    
    .final-message-block {
        padding: 30px 20px;
    }
    
    .cta-buttons {
        flex-direction: column;
    }
    
    .btn-primary,
    .btn-secondary {
        width: 100%;
    }
}

@media (max-width: 480px) {
    .header-title {
        font-size: 24px;
    }
    
    .content-block h2 {
        font-size: 20px;
    }
    
    .final-message-block {
        padding: 20px;
    }
    
    .quote-main {
        font-size: 20px;
    }
}
---SEPARATOR---
// ===== MODAL VIDEO PLAYER =====
function openModal() {
    const modal = document.getElementById('videoModal');
    if (!modal) {
        console.error('Modal element not found');
        return;
    }
    
    modal.classList.add('active');
    document.body.style.overflow = 'hidden';
    
    // Load video when modal opens
    loadVideoBlob().then(() => {
        const video = document.getElementById('mainVideo');
        if (video && video.tagName === 'VIDEO') {
            const playPromise = video.play();
            if (playPromise !== undefined) {
                playPromise.catch(error => {
                    console.log('Autoplay prevented:', error);
                });
            }
        }
    }).catch(error => {
        console.error('Error in openModal:', error);
    });
}

function closeModal() {
    const modal = document.getElementById('videoModal');
    if (!modal) return;
    
    modal.classList.remove('active');
    const video = document.getElementById('mainVideo');
    if (video && video.tagName === 'VIDEO') {
        video.pause();
        video.currentTime = 0;
    }
    document.body.style.overflow = 'auto';
}

// Close modal when clicking outside the content
document.addEventListener('DOMContentLoaded', function() {
    const modal = document.getElementById('videoModal');
    
    window.addEventListener('click', function(event) {
        if (event.target == modal) {
            closeModal();
        }
    });
});

// Close modal with Escape key
document.addEventListener('keydown', function(event) {
    if (event.key === 'Escape') {
        closeModal();
    }
});

// ===== NAVBAR =====
const hamburger = document.querySelector('.hamburger');
const navMenu = document.querySelector('.nav-menu');
const navLinks = document.querySelectorAll('.nav-link');

if (hamburger) {
    hamburger.addEventListener('click', () => {
        navMenu.classList.toggle('active');
        hamburger.classList.toggle('active');
    });
}

navLinks.forEach(link => {
    link.addEventListener('click', () => {
        navMenu.classList.remove('active');
        if (hamburger) hamburger.classList.remove('active');
    });
});

// ===== SMOOTH SCROLL =====
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    });
});

// ===== INITIALIZE AOS =====
document.addEventListener('DOMContentLoaded', function() {
    AOS.init({
        duration: 800,
        easing: 'ease-in-out',
        once: false,
        mirror: true,
        offset: 100
    });
});

// ===== NAVBAR SCROLL EFFECT =====
const navbar = document.querySelector('.navbar');
let lastScroll = 0;

window.addEventListener('scroll', () => {
    const currentScroll = window.pageYOffset;
    
    if (currentScroll > 100) {
        navbar.style.background = 'linear-gradient(to bottom, rgba(5, 5, 5, 0.99), rgba(5, 5, 5, 0.85))';
        navbar.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.5)';
    } else {
        navbar.style.background = 'linear-gradient(to bottom, rgba(5, 5, 5, 0.98), rgba(5, 5, 5, 0.8))';
        navbar.style.boxShadow = 'none';
    }
    
    lastScroll = currentScroll;
});

// ===== HERO BUTTON RIPPLE EFFECT =====
const buttons = document.querySelectorAll('.hero-btn');

buttons.forEach(button => {
    button.addEventListener('mousedown', function(e) {
        const ripple = document.createElement('span');
        ripple.style.width = ripple.style.height = '20px';
        ripple.style.background = 'rgba(255, 255, 255, 0.5)';
        ripple.style.borderRadius = '50%';
        ripple.style.position = 'absolute';
        ripple.style.transform = 'scale(0)';
        ripple.style.animation = 'ripple 0.6s ease-out';
        ripple.style.pointerEvents = 'none';
        
        const rect = this.getBoundingClientRect();
        ripple.style.left = (e.clientX - rect.left) + 'px';
        ripple.style.top = (e.clientY - rect.top) + 'px';
        
        this.style.position = 'relative';
        this.appendChild(ripple);
        
        setTimeout(() => ripple.remove(), 600);
    });
});

// ===== PARALLAX SCROLL EFFECT =====
window.addEventListener('scroll', () => {
    const scrollY = window.pageYOffset;
    const heroVideo = document.querySelector('.hero-video');
    
    if (heroVideo) {
        heroVideo.style.transform = `translateY(${scrollY * 0.5}px)`;
    }
});

// ===== CONSOLE MESSAGE =====
console.log(
    '%cLARA TOLOSA',
    'font-size: 24px; font-weight: bold; color: #b30000; text-shadow: 0 0 10px rgba(15, 76, 129, 0.5);'
);
console.log(
    '%cSi lo ves, actÃºaâ€¦ No te calles.',
    'font-size: 16px; color: #0f4c81; font-style: italic;'
);
console.log(
    '%cUn cortometraje sobre conciencia social',
    'font-size: 12px; color: #999;'
);

// ===== LOAD VIDEO AS BLOB =====
async function loadVideoBlob() {
    try {
        const videoElement = document.getElementById('mainVideo');
        if (!videoElement) {
            console.error('Video element not found');
            return;
        }
        
        // Try multiple video sources
        const videoSources = [
            'https://cdn.jsdelivr.net/gh/ErCapatazDebStudio/web-lt@main/assets/Lara_Tolosa_ultra.mp4',
            'https://cdn.jsdelivr.net/gh/ErCapatazDebStudio/web-lt@main/assets/Lara_Tolosa_compressed.mp4',
            'https://cdn.jsdelivr.net/gh/ErCapatazDebStudio/web-lt@main/assets/Lara%20Tolosa.mp4'
        ];
        
        for (const videoUrl of videoSources) {
            try {
                console.log(`Attempting to load video from: ${videoUrl}`);
                const response = await fetch(videoUrl, { 
                    mode: 'cors',
                    method: 'GET'
                });
                
                if (response.ok) {
                    console.log(`Video source successful: ${videoUrl}`);
                    const blob = await response.blob();
                    const blobUrl = URL.createObjectURL(blob);
                    
                    // Clear existing content
                    videoElement.innerHTML = '';
                    
                    // Create and add source element
                    const sourceElement = document.createElement('source');
                    sourceElement.src = blobUrl;
                    sourceElement.type = 'video/mp4';
                    videoElement.appendChild(sourceElement);
                    videoElement.load();
                    
                    console.log('Video loaded successfully from blob');
                    return;
                }
            } catch (error) {
                console.warn(`Failed to load from ${videoUrl}:`, error.message);
            }
        }
        
        // If all sources fail
        throw new Error('All video sources failed to load');
    } catch (error) {
        console.error('Error loading video:', error);
        const videoElement = document.getElementById('mainVideo');
        if (videoElement) {
            videoElement.innerHTML = '<p style="color: #999; padding: 20px; text-align: center; font-family: Poppins, sans-serif;">Lo siento, el video no estÃ¡ disponible en este momento. Por favor, recarga la pÃ¡gina e intenta de nuevo.</p>';
        }
    }
}

